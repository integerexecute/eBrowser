<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="eBrowser.Windows.Views.Pages.ViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:eBrowser.Windows.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="CompactListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="12" Spacing="12">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnBack"
                    Content="◀"
                    Grid.Column="0"
                    Click="BtnBack_Click"
                    HorizontalAlignment="Left"/>

                <TextBlock x:Name="IndexLabel"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Text="1 / 10" />

                <Button x:Name="BtnNext"
                    Content="▶"
                    Grid.Column="2"
                    Click="BtnNext_Click"
                    HorizontalAlignment="Right"/>
            </Grid>
            <TextBlock x:Name="DimensionLabel" Text="Dimension: None"/>
            <TextBlock x:Name="ExtensionLabel" Text="Extension: exe"/>
            <TextBlock x:Name="FileSizeLabel" Text="File Size: 2147483647 B"/>
            <TextBlock x:Name="IdLabel" Text="ID: 69420"/>
            <ListBox x:Name="Artists" Height="50" ItemContainerStyle="{StaticResource CompactListBoxItemStyle}"/>
            <ListBox x:Name="Characters" Height="100" ItemContainerStyle="{StaticResource CompactListBoxItemStyle}"/>
            <ListBox x:Name="Tags" Height="200" ItemContainerStyle="{StaticResource CompactListBoxItemStyle}"/>
            <ListBox x:Name="Sources" Height="50" ItemContainerStyle="{StaticResource CompactListBoxItemStyle}"/>
        </StackPanel>
        <WebView2 Grid.Column="1" x:Name="webView"/>
    </Grid>
</Page>
